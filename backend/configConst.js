const port = 5000;
const nameDirectoryRequests = 'httpRequests'; // nombre de la carpeta que contiene las peticiones de cada tabla
const nameDirectoryDicom = 'dicomEstudios'; // nombre de la carpeta que contiene los archivos dicom

module.exports = { 
    port, 
    nameDirectoryRequests, 
    nameDirectoryDicom 
};